# New Conversation Prompt — Document Vision Topic Scout
Continue the attached verified Topic Scout project.

This is a CONTINUATION handoff. After verification, this Chat becomes the active project conversation.

Before research:
1. identify handoff and parent IDs;
2. confirm project short name;
3. confirm this Chat's continuation sequence equals the sender's recorded next sequence;
4. confirm expected title and active Q ID;
5. list files actually readable;
6. compare actual files with `HANDOFF_MANIFEST.md`;
7. read `START_HERE.md` and authoritative state;
8. confirm current phase, natural migration point, context risk, and first task;
9. stop on missing/conflicting critical state; do not reconstruct from memory.

Only after checks report `CONTINUATION ACCEPTED`.
If blocked report `HANDOFF BLOCKED`.

Preserve search envelope, closest prior work, gap status, feasibility status, and user-frozen scope.
At the next natural migration point or material context risk, reassess migration.
Do not open another Chat until a new bundle is actually generated and verified.
